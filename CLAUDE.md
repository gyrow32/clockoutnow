# CLAUDE.md

This repo contains both **code** and **operations docs** for an outbound workflow:

- Next.js admin dashboard + preview pages: `website/`
- Python automation scripts: `scripts/`
- Ops docs: `docs/`

Agent guidance is centralized in `AGENTS.md`.

